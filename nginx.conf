

http {
   server {
      listen 80;

      server_name 45.10.244.78;

      location / {
         proxy_pass http://frontend:3000;
      }

      location /api {
         proxy_pass http://backend:3001;
      }
   }
}